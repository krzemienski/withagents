## Featured in

**"The tunnel config is the whole product" — withagents.dev Day 44.**
Why opencode-mobile is a four-line JSON tunnel config, not a fork. The mode-bet lesson: SDK-mode products get a tunnel + auth policy for mobile, not a second binary. Three failed attempts (native iOS client, ngrok, Tailscale) that taught it.

Read: https://withagents.dev/posts/day-44-opencode-mobile

Pattern lives at `~/.config/opencode-mobile/tunnel-config.json`. No separate repo — the tunnel contract is the whole patch.
