## Featured in

**"A PRD is a contract between an intent and an agent" — withagents.dev Day 46.**
Why claude-code-prd-creator ships as a pre-code-access session (not an MCP tool inside a normal Claude Code session). The four-document split. The two interview questions that rule every downstream decision. Why 200+ granular tasks beat "build the backend" every time.

Read: https://withagents.dev/posts/day-46-claude-code-prd-creator

Outputs: Main PRD + AI Custom Instructions + Design System + Task Breakdown. Claude API primary, OpenAI fallback. Production-only testing posture enforced from the AI instructions document forward.
