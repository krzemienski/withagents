## Featured in

**"The diagram is a spec, not a decoration" — withagents.dev Day 47.**
Why live-mermaid-editor validates before it paints: 300ms debounce, syntax check before render, WebSocket pushes only validated state. The export path pins one Mermaid library version so the diagram you see is the SVG you ship — no re-parse drift. Seven templates bundled because blank canvas kills diagrams.

Read: https://withagents.dev/posts/day-47-live-mermaid-editor

`npm install && npm start` → http://localhost:3200. Core server in TypeScript under `src/core.ts`.
