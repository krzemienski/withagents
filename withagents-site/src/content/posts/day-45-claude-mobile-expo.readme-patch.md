## Featured in

**"fetch().getReader() hangs forever in React Native" — withagents.dev Day 45.**
The React Native SSE bug that cost a week, the XMLHttpRequest `readyState === 3` fallback, and why claude-mobile-expo exists as a real binary (not a tunnel wrapper) because Claude Code on mobile is Interactive-mode — skills, MCP config, session state, hooks. 84/99 gate tests passing against a real CLI subprocess. Not mocks.

Read: https://withagents.dev/posts/day-45-claude-mobile-expo

v2.0.0 — React Native + FastAPI, OpenAI-compatible API, 5 hard gates (P1, F1, F2, I1, I2).
