# README patch — day-04-orchestration-topology

Target repo: `trace-timeline` (open-source index under withagents.dev / Open Source).

Add under an existing `## Featured in` heading (create the section if it does not exist yet):

---

## Featured in

**Orchestration topology is the product** — WithAgents, April 2026.

`trace-timeline` is used in the post as the reference tool for rendering the real topology of a running agent fleet from its session log. The 45-day push produced a three-shape-nested composition (wave outer, star inner, loops nested inside two of the wave's nodes) that was not planned up front; `trace-timeline` made it legible after the fact.

Read: https://withagents.dev/writing/day-04-orchestration-topology
