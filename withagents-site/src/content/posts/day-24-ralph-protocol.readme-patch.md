# README patch — day-24-ralph-protocol

Target repo: `ralph-loop-patterns` (canonical, github.com/krzemienski/ralph-loop-patterns). Also applicable to `agent-contracts` (the OSS extraction).

Add under an existing `## Featured in` heading (create the section if it does not exist):

---

## Featured in

**The RALPH protocol** — WithAgents, May 2026.

The third and closing post of the 64-day Ralph arc. Specifies the protocol that emerged from the iOS forcing function (Day 23): five invariants (one exit event per hat, append-only disk facts, event-gated transitions, typed directives, convergence-only termination), the shared event envelope, and three things the protocol deliberately does not solve (consent races, offline reconciliation, cross-project topology).

The protocol is extracted as `agent-contracts` — the reusable contract layer applicable to any hat-rotating agent loop.

Read: https://withagents.dev/writing/day-24-ralph-protocol
