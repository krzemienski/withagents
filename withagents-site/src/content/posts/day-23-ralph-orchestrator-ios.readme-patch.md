# README patch — day-23-ralph-orchestrator-ios

Target repo: `ralph-orchestrator-ios` (SwiftUI app + Node bridge daemon; repo separation question still open — if shipped as subfolder inside `ralph-orchestrator`, apply this patch to that repo's README under a `## iOS` section).

Add under an existing `## Featured in` heading (create the section if it does not exist):

---

## Featured in

**Ralph, but on the phone** — WithAgents, May 2026.

The middle of the 64-day Ralph arc. Covers the command grammar, the three-column scan-to-decide digest line, the 240-character guidance cap, the `memo:` extension for longer directives, and the three honest breakages (SSE network flap recovery, cap-too-tight cases, consent-race between phone guidance and laptop review.verdict).

The RALPH protocol that emerged from this forcing function is documented in the Day 24 post.

Read: https://withagents.dev/writing/day-23-ralph-orchestrator-ios
