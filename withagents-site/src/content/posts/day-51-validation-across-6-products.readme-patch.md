# README patch — day-51-validation-across-6-products

Target surface: cross-cutting. No single companion repo owns this post — it is a Week-8 meta-post across six products. Use this patch text for any of the six canonical repos that want to cite it.

Suggested target(s):

- `validationforge` (the rule as product — highest fit)
- `sessionforge` (the rule restated for receipts)
- `ralph-loop-patterns` (the rule restated for termination)
- `code-tales` / `code-tales-platform` (the rule on a consumer product)
- `ils-ios` (the rule enforced by the tool surface)
- `remodex` (the rule across a process boundary)

Add under an existing `## Featured in` heading (create the section if it does not exist):

---

## Featured in

**Validation across six products** — WithAgents, June 2026.

Week-8 meta-post published after the Day 50 flagship finale. Argues that ValidationForge, SessionForge, Ralph, Code Stories, ILS-iOS, and Remodex are six restatements of the same rule — "a build that compiles is not a feature that works" — across six different terrains, and distills the shared three-layer response (make the shortcut unavailable, keep evidence on disk, let the operator read the evidence not the agent's summary).

The OSS extractions — `agent-contracts`, `trace-timeline`, `context-layers` — are one primitive per layer.

Read: https://withagents.dev/writing/day-51-validation-across-6-products
